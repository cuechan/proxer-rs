pub struct Tag {
    pub tag_id:       u32,
    pub id:           u32,
    pub timestamp:    u32,
    pub rate_flag:    bool,
    pub spoiler_flag: bool,
    pub tag:          String,
    pub description:  String,
}
