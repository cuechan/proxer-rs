pub struct Group {
    pub id: u32,
    pub name: String,
    pub link: String,
    pub country: String,
    pub image: String,
    pub description: String,
    pub count: String,
    pub cprojects: String, // what is this?
}
